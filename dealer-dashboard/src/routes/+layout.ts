// The dashboard is a client-driven console backed by an in-memory mock API,
// so we render as a single-page app (no SSR / no prerendering).
export const ssr = false;
export const prerender = false;
