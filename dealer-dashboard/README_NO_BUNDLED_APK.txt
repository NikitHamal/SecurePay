The old bundled tb-user-latest.apk was removed from this patched dashboard zip.
Provisioning must use the immutable APK URL and checksums from R2 latest.json,
after the patched TB User APK is built and published by GitHub Actions.
