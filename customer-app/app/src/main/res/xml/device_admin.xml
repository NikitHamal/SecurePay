<?xml version="1.0" encoding="utf-8"?>
<!-- Policies requested by the SecurePay device-financing DPC.
     These map to DevicePolicyManager capabilities used to enforce the
     LOCKED state until the financed balance is settled. -->
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <force-lock />
        <disable-keyguard-features />
        <watch-login />
    </uses-policies>
</device-admin>
